name: ci_build

on:
  push:
    paths:
      - "scripts/CMakeLists.txt"
      - ".github/workflows/ci_build.yml"
      - "cmake/libraries.json"

jobs:

  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        scotch: [true]

    env:
      FC: gfortran-14

    steps:
    - uses: actions/checkout@v6

    - name: Install prereqs (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt update
        sudo apt install --no-install-recommends libopenmpi-dev openmpi-bin liblapack-dev
        [[ "${{ matrix.scotch }}" == "true" ]] && sudo apt install --no-install-recommends bison flex

    - name: Install prereqs (macOS)
      if: runner.os == 'macOS'
      run: |
        brew install open-mpi
        [[ "${{ matrix.scotch }}" == "true" ]] && brew install bison flex

    - name: build, test release
      if: ${{ !matrix.scotch }}
      run: cmake --workflow default

    - name: build with scotch
      if: ${{ matrix.scotch }}
      run: cmake --workflow scotch

    - run: cmake --install build-scotch

# builds fine on local machine
    # - name: configure example
    #   run: cmake -S example -B example/build -DBUILD_SHARED_LIBS:BOOL=${{ matrix.shared }}
    # - name: build example
    #   run: cmake --build example/build
    # - name: test example
    #   run: ctest --test-dir example/build -V
