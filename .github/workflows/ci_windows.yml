name: ci_windows

env:
  CMAKE_BUILD_TYPE: Release

on:
  push:
    paths:
      - "**.cmake"
      - "**/CMakeLists.txt"
      - ".github/workflows/ci_windows.yml"


jobs:

  windows:
    runs-on: windows-latest
    timeout-minutes: 15
    env:
      CMAKE_GENERATOR: MinGW Makefiles

    steps:
    - uses: msys2/setup-msys2@v2
      with:
        update: true
        install: >-
          mingw-w64-x86_64-gcc-fortran
          mingw-w64-x86_64-lapack

    - name: Put MSYS2_MinGW64 on PATH
      # this is because GNU Patch doesn't work from MSYS2 shell
      run: echo "D:/a/_temp/msys64/mingw64/bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

    - uses: actions/checkout@v2

    - run: cmake -B build -Darith=d -Dparallel:BOOL=false -DCMAKE_INSTALL_PREFIX=${{ runner.temp }}
    - run: cmake --build build --parallel
    - run: ctest --test-dir build --preset default

    - name: install package
      run: cmake --install build

    - name: configure example
      run: cmake -S examples -B examples/build -DCMAKE_PREFIX_PATH=${{ runner.temp }} -Dparallel:BOOL=false
    - name: build example
      run: cmake --build examples/build || cmake --build examples/build -v
    - name: test example
      run: ctest --test-dir examples/build -V
